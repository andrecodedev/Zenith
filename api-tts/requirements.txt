fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3
pydantic-core==2.27.1
pydantic-settings==2.6.1
aiofiles==24.1.0
# Motor: OmniVoice (k2-fsa), clone de voz zero-shot. CPU ok, lento sem GPU CUDA/MPS.
# Torch CPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# omnivoice puxa gradio como dependência obrigatória (só usado no demo web dele,
# não usamos aqui) e isso trava a resolução com fastapi/pydantic pinados acima.
# Por isso ele é instalado à parte, sem deps:
#   pip install --no-deps omnivoice==0.2.1
# Os deps reais que a gente usa ficam explícitos abaixo.
transformers>=5.3.0
accelerate
pydub
soundfile>=0.12.1
